_BASE_: "./Base_waymo.yaml"

MODEL:
  DEPTH_NET:
    NAME: "GoogleResNetv2"
    ENCODER_NAME: "18??"
    UPSAMPLE_DEPTH: False
    LEARN_SCALE: False
    NORM: 'randLN'
    NOISE_STDDEV: 0.5
    RAMPUP_ITERS: 10000
  POSE_NET:
    SCALE_CONSTRAIN: "clip_ste"
SOLVER:
  IMS_PER_BATCH: 16
  LR_STEPS: (200,)
  GAMMA: 0.5
  MAX_EPOCHS: 200
DATALOADER:
  NUM_WORKERS: 8
